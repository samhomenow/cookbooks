#!/bin/bash

STAMP=`date +%d%m%Y`
LOG=/var/log/aide/aide

[[ -e /usr/sbin/aide ]] && aide="/usr/sbin/aide"

${aide:-aide} --check > $LOG.${STAMP}  

gzip $LOG.${STAMP}

find /var/log/aide -ctime +30 -exec rm -rf {} \;

